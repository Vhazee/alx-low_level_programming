#include "main.h"

/**
 * print_alphabet - print all alphabet lowercase
 */
void print_alphabet(void)
{
	char i;
	/*For loop to loop through all the alphabets*/
	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);
	_putchar('\n');
}
