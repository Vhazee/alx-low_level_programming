#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int i;
	/**For loop to loop from 0-10**/
	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
